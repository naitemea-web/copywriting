# agents.md — 카피 마스터 작업 규칙 (AI 코딩 에이전트용)

> Claude Code · Cursor 등 AI 코딩 에이전트가 이 저장소에서 작업할 때 따르는 규칙.
> 사람 기여자도 동일하게 따른다. **작업 전 반드시 이 문서 → 관련 단계의 `todo.md` 순으로 읽는다.**

---

## 0. 문서 지도 (Source of Truth)

| 문서 | 역할 | 변경 시 |
|------|------|---------|
| `PRD.md` | 무엇을·왜 (제품 요구·학습 과학 원칙·KPI) | 제품 방향이 바뀔 때만 |
| `spec.md` | 어떻게 (기술 스택·데이터 구조·로직·화면) | 구현 결정이 바뀔 때 |
| `todo.md` | 순서 (1~10단계 체크리스트·DoD) | 단계 완료/추가 시 |
| `design.md` | 디자인 기준 (토큰·컴포넌트·반응형) | 비주얼 규칙이 바뀔 때 |
| `agents.md` | 작업 규칙 (이 문서) | 프로세스가 바뀔 때 |

**충돌 시 우선순위**: `PRD.md` > `spec.md` > `design.md` > 코드. 코드가 문서와 어긋나면 코드를 고치거나, 문서를 먼저 갱신하고 코드를 맞춘다(둘 중 무엇이 옳은지 불명확하면 사람에게 질문).

---

## 1. 핵심 원칙 (절대 규칙)

1. **학습 과학 메커닉을 훼손하지 않는다.** 인출(①)·분산(②)·인터리빙(③)·메타인지(⑦)는 이 제품의 정체성이다.
   - 특히 **인출 게이트**: 사용자가 스스로 떠올리기 전에 정답/보기를 노출하는 변경은 금지.
2. **AI는 카피를 대신 쓰지 않는다.** 미션 생성·피드백·진단은 허용, **카피 본문 대필은 영구 금지**(원리⑥). 이 경계를 무너뜨리는 기능은 추가하지 않는다.
3. **MVP 범위를 지킨다.** `spec.md §1.2` Out of Scope(서버 DB·계정·결제·멀티플레이)는 요청 없이 도입하지 않는다.
4. **디자인 토큰 밖 값을 쓰지 않는다.** 색·간격·반경·타이포는 `design.md`/`tailwind.config.js` 토큰만 사용. 임의 hex·px 금지.
5. **순수 함수 우선.** `lib/*`의 학습 로직은 부수효과 없는 순수 함수로, `now`/시드를 주입받아 테스트 가능하게 작성한다. `Date.now()`/`Math.random()`을 로직 내부에서 직접 호출하지 않는다.

---

## 2. 작업 흐름 (Workflow)

1. **읽기**: `agents.md` → 해당 `todo.md` 단계 → 관련 `spec.md`/`design.md` 절.
2. **한 단계씩**: `todo.md`의 한 단계(또는 한 체크박스)를 단위로 작업한다. 단계를 건너뛰지 않는다(의존성 1→2→…→10).
3. **구현**: 주변 코드의 컨벤션·주석 밀도·네이밍을 따른다.
4. **검증**: `npm run typecheck && npm run test && npm run build` 를 통과시킨다.
5. **체크**: 끝낸 항목은 `todo.md`에서 `[ ]`→`[x]` 로 갱신한다.
6. **커밋**: 아래 커밋 규칙을 따른다.

> 막히거나 모호하면(스펙 해석이 갈리거나, 아키텍처에 영향이 크면) 임의 결정하지 말고 **질문**한다.

---

## 3. 코드 컨벤션

- **언어/스택**: TypeScript strict, React 18 함수형 컴포넌트 + 훅. 클래스 컴포넌트 금지.
- **임포트 별칭**: `@/` = `src/`. 상대경로 `../../` 남발 금지.
- **파일 배치**: `spec.md §7` 디렉토리 구조를 따른다.
  - `data/` 정적 시드 · `types/` 타입 · `lib/` 순수 로직 · `store/` Zustand · `components/` 재사용 UI · `pages/` 라우트 화면.
- **상태**: 전역은 Zustand(`store/gameStore.ts`), 로컬 UI 상태는 `useState`. 진행도/SRS는 반드시 store를 통해 영속.
- **네이밍**: 컴포넌트 PascalCase, 함수/변수 camelCase, 타입/인터페이스 PascalCase, 상수 UPPER_SNAKE.
- **주석**: 한국어. "무엇"이 아니라 "왜"를 설명. 학습원리 관련 코드엔 `// ① 인출` 처럼 원리 번호를 단다.
- **금지**: `any`(불가피하면 `unknown`+가드), 미사용 변수(`noUnusedLocals`), 인라인 매직 넘버(상수화).

---

## 4. 데이터·콘텐츠 규칙

- 카드/섹션/미션/퀴즈 시드는 `data/*.ts`에만 둔다. 컴포넌트에 콘텐츠를 하드코딩하지 않는다.
- 모든 참조 id(`recommendedCardIds`, `answerCardId`, `choiceCardIds`)는 실제 카드 id와 일치해야 한다(2단계 무결성 점검).
- 카피 예시는 「카피라이팅 바이블」 출처를 따르되, 새 예시 추가 시 기존 톤·길이를 유지한다.

---

## 5. 테스트 규칙

- **반드시 단위 테스트**: `lib/scoring` · `lib/srs` · `lib/calibration` · `lib/deck` · `lib/session`(시간/시드 의존 함수 포함).
- 시간 의존 테스트는 고정 타임스탬프를 주입한다(`srs.test.ts` 참고).
- 채점·SRS·보정 로직을 수정하면 해당 테스트를 함께 갱신/추가한다.
- PR/커밋 전 `npm run test` 그린 필수.

---

## 6. 커밋 & 브랜치

- **브랜치**: `claude/copywriting-game-spec-reij7s` (지정 브랜치). 다른 브랜치 푸시 금지.
- **커밋 메시지**: 한 줄 요약(명령형) + 본문(무엇/왜). 단계 표기 권장: `feat(M3): 9섹션 빌더 드래그 배치 구현`.
- **단위**: 논리적으로 완결된 작은 단위로 커밋. 빌드 깨진 상태로 커밋하지 않는다.
- **PR**: 사용자가 명시적으로 요청할 때만 생성한다.

---

## 7. 명령어 (cheat sheet)

```bash
npm install          # 의존성 설치
npm run dev          # 개발 서버
npm run typecheck    # 타입 체크 (tsc --noEmit)
npm run test         # 단위 테스트 (vitest run)
npm run build        # 프로덕션 빌드 (tsc -b && vite build)
npm run lint         # ESLint
```

**완료 게이트(Definition of Done, 모든 단계 공통)**: `typecheck` + `test` + `build` 3종 그린 + `design.md` 토큰 준수 + 해당 단계 `todo.md` DoD 충족.

---

## 8. 하지 말 것 (Don't)

- ❌ 인출 게이트 우회(정답 선노출), AI 카피 대필, 미드그레이 텍스트 도입.
- ❌ 토큰 밖 색/간격/반경 사용, 사각형 버튼(모든 CTA는 pill).
- ❌ MVP 범위 밖 기능(서버·계정·결제) 무단 추가.
- ❌ 로직 함수 내부에서 `Date.now()`/`Math.random()` 직접 호출.
- ❌ 문서(`PRD`/`spec`/`design`)와 어긋나는 구현을 설명 없이 머지.
- ❌ 한 커밋에 여러 단계를 뭉뚱그리기, 빌드 깨진 채 커밋.
